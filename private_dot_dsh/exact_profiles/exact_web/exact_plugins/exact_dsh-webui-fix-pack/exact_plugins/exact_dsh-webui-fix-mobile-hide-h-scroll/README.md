# `dsh-webui-fix-mobile-hide-h-scroll`

> **已不再需要（deprecated）**
>
> 该问题已经由上游 [`lehhair/dsh-mobile#2`](https://github.com/lehhair/dsh-mobile/pull/2) 修复：上游在 coarse-pointer 移动端布局下同时隐藏了 `html[data-dsh-mobile]` 根滚动条及其 WebKit scrollbar。
>
> 使用当前版本的 `dsh-mobile`（包含该 PR）时，不需要再安装本插件。保留本插件仅用于旧版 `dsh-mobile`。

This plugin is retained only for older `dsh-mobile` releases. With a current upstream version, install [`dsh-mobile#2`](https://github.com/lehhair/dsh-mobile/pull/2) instead and do not install this plugin.
