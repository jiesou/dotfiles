// Node half: all work happens in the browser (see lib/client.js).
// The empty apply keeps the bundle row loadable on the Node side.
export const name = '@jiesou/dsh-webui-fix-session-list-frame-coalesce'

export function apply() {}
